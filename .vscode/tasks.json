{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "deploy",
            "type": "shell",
            "command": "~/Sites/stati/stati/bin/stati build -vvv && rsync -avv _site/* pi@192.168.1.2:/var/www/custom/",
            "problemMatcher": []
        },
        {
            "label": "build",
            "type": "shell", 
            "command": "~/Sites/stati/stati/bin/stati build"
        }
    ]
}